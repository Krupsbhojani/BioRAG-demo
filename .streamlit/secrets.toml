# Streamlit Community Cloud secret storage.
# On Streamlit Cloud: paste your key in the app's Secrets settings panel.
# For local development: copy .env.example to .env instead.
#
# GROQ_API_KEY = "gsk_..."
